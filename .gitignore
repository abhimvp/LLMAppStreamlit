# --- Python Virtual Environments ---
# Usually contains installed packages specific to the local setup
venv/
.venv/
.env
env/
ENV/
env.bak/
venv.bak/
.env # Often contains secrets, check if you NEED to track a template like .env.example

# --- Python Bytecode and Cache ---
# These are generated automatically by the interpreter
__pycache__/
*.pyc
*.pyo
*.pyd
*.pyz
*.pyzw

# --- Distribution / Packaging ---
# Artifacts created when building packages
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# --- IDE / Editor Configuration ---
# User-specific settings, should not be shared
.idea/           # PyCharm, IntelliJ
.vscode/         # VS Code (unless you want to share specific settings like launch.json, then refine)
*.sublime-project
*.sublime-workspace
*.komodoproject
.spyderproject
.spyproject

# --- Testing ---
# Files generated by testing frameworks and coverage tools
.pytest_cache/
.coverage
.coverage.*
htmlcov/
nosetests.xml
coverage.xml
*.cover
pytestdebug.log

# --- Jupyter Notebook ---
# Checkpoints created automatically
.ipynb_checkpoints

# --- Operating System Files ---
# Files generated by macOS, Windows, Linux
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# --- Secrets / Sensitive Data ---
# NEVER commit secrets. Use environment variables or secure secret management.
# Add any files containing API keys, passwords, database credentials, etc.
*.key
*.pem
credentials*.*
secrets*.*
config.local.yaml # Example for local configs

# --- Logs ---
# Application or system logs
*.log
logs/

# --- Databases ---
# Local database files (unless it's a small, intentional part of the repo)
*.db
*.sqlite
*.sqlite3

# --- Instance folders (Common in Flask/Django) ---
# Often contains local configuration or databases
instance/
/instance/ # Use leading slash to match only in root

# --- Other specific project files (Add as needed) ---
# Example: Temporary files from specific libraries
# .webassets-cache/
# .scrapy/

# If you have files you DO want to track despite a rule above, use !
# Example: Keep a specific configuration file within .vscode
# !.vscode/launch.json